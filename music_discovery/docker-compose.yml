version: '3.8'

services:
  endee:
    image: endeeio/endee-server:latest
    container_name: endee-music-db
    ports:
      - "8080:8080"
    volumes:
      - endee_music_data:/data
    environment:
      - NDD_DATA_DIR=/data
    restart: unless-stopped

volumes:
  endee_music_data:
